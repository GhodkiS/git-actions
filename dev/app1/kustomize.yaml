# lock target environment starts
- target:
    kind: Application
    name: app1
  patch: |-
     - op: replace
       path: /spec/source/targetRevision
       value: branch2
# lock target environment ends
# lock target environment starts
- target:
    kind: Application
    name: app1
  patch: |-
     - op: replace
       path: /spec/source/targetRevision
       value: branch2
# lock target environment ends
# lock target environment starts
- target:
    kind: Application
    name: app1
  patch: |-
     - op: replace
       path: /spec/source/targetRevision
       value: branch1
# lock target environment ends
# lock target environment starts
- target:
    kind: Application
    name: app1
  patch: |-
     - op: replace
       path: /spec/source/targetRevision
       value: branch1
# lock target environment ends
# lock target environment starts
- target:
    kind: Application
    name: app1
  patch: |-
     - op: replace
       path: /spec/source/targetRevision
       value: branch1
# lock target environment ends
